{
  "name": "live-streaming-sports",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "echo 'Use Vercel CLI: vercel dev'",
    "generate-admin-hash": "node -e \"const bcrypt = require('bcryptjs'); console.log(bcrypt.hashSync(process.argv[1] || 'change-me', 10))\""
  },
  "dependencies": {
    "bcryptjs": "^2.4.3"
  }
}
